on_actions = {    
    on_state_control_changed = {
		effect = {
			if = {
				limit = { tag = LEP }
				if = {
					limit = {
						FROM.FROM = { state = 608 } #Beijing
						has_country_flag = LEP_dem_gov
					}
					country_event = { id = league_mid.28 days = 3 } 
				}
			}
		}
	}

	on_unit_leader_level_up = {
		effect = {
			if = {
				limit = {
					FROM = { tag = LEP }
				}
				unit_leader_event = league_generic.7 #Champion Event
			}
		}
	}

	
}